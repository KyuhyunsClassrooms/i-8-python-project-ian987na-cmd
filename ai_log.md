# Gemini 활용 기록

> 이번 수행평가에서는 학교 계정 Gemini 사용을 허용합니다.  
> 단, Gemini가 만든 코드를 이해하지 못한 채 그대로 제출하면 안 됩니다.  
> 실제 개인정보, 친구 이름, 성적, 전화번호, 주소 등은 절대 입력하지 않습니다.

---

## 1차 Gemini 활용

- 사용 날짜: 6/11
- 사용 목적: 2차원 리스트의 설계, 함수 설계, 조건문을 어디에 쓰는지 물어봄
- 내가 입력한 프롬프트:

```text
여기에 내가 Gemini에게 입력한 질문을 붙여넣기
```고등학교 정보 수준에서 2차원 리스트와 함수 분리를 쓸 수 있는 파이썬 프로젝트 주제를 추천해 줘.
'우리 동네 미세먼지 및 대기오염 경보 시스템' 주제의 입력, 처리, 출력 구조와 2차원 리스트 구조를 어떻게 잡아야 할지 알려줘.
이 프로그램에서 반복문과 조건문은 각각 어느 부분에 사용되고, 발생할 수 있는 예외 상황은 무엇이 있어?

- Gemini가 제안한 내용 요약:
주제 및 데이터 구조: 요일별(행), 시간대별(열) 미세먼지 농도를 관리하는 2차원 리스트(dust_data) 구조 제안.

함수 분리: 기능을 print_dust_table(data)(출력), get_average(day_data)(평균 계산), check_warning(avg_value)(등급 판정) 3개로 분리하는 구조 제안.

제어 구조 및 예외 처리: 데이터를 표로 출력할 때와 평균을 구할 때 for 반복문을 활용하고, 미세먼지 수치 등급 판정 시 if-elif-else 조건문을 활용함. 사용자가 요일 범위를 벗어난 번호(0~4 외)를 입력할 때를 대비한 예외 처리 조건문 구조를 안내함.
- 내가 반영한 부분:AI가 제안한 '우리 동네 미세먼지 및 대기오염 경보 시스템'의 기본 데이터 구조와 함수 분리 설계를 그대로 채택함. AI가 제공한 뼈대 코드를 바탕으로 등급별 판정 조건문(if-elif-else)의 세부 수치 기준(35 이하 좋음, 75 이하 보통 등)을 검토하고, 메인 흐름에서 인덱스 에러(IndexError)를 방지하기 위한 예외 처리 조건문 코드를 직접 확인하고 적용함.
- 내가 수정하거나 사용하지 않은 부분:
- 반영 위치: project_plan.md의 데이터 리스트와 함수, main.py(멘트 및 출력형태)

---

## 2차 Gemini 활용

- 사용 날짜: 6/12
- 사용 목적: 예외 상황 분석과 전체 프로그램의 뼈대 구조 확인, 그리고 이전 차시 내용 복습
- 내가 입력한 프롬프트:
 우리 프로그램(미세먼지 경보 시스템)에서 예상되는 예외 상황이 있을까?

: 반복문이 구체적으로 코드의 어느 부분들에 사용되는지 설명해 줘.

: 프로그램의 전체적인 뼈대 코드를 보여주고 구조를 설명해 줘.

```text

```

- Gemini가 제안한 내용 요약:
예외 상황 분석: 사용자가 요일 선택 시 범위를 벗어난 숫자(0~4 이외의 값)를 입력하여 리스트 인덱스 에러(IndexError)가 발생하는 상황을 지적하고, 이를 방지할 조건문 구조를 제안함.

반복문 활용 구체화: 전체 데이터를 표로 출력할 때 사용하는 for문과, 특정 요일의 [오전, 오후, 야간] 수치를 모두 더해 평균을 구할 때 사용하는 for문의 역할을 각각 분리하여 설명함.

가이드 코드 제공: 함수 호출 흐름(입력 -> 예외 체크 -> 평균 계산 -> 등급 판정 및 출력)이 한눈에 보이는 뼈대 코드를 제공함.
- 내가 반영한 부분:AI가 짚어준 예외 상황(인덱스 범위를 벗어난 입력)의 심각성을 인지하고, 이를 방지하기 위한 예외 처리 조건문(if day_index < 0 or day_index > 4:) 코드를 메인 흐름에 직접 적용함. AI의 설명을 통해 for문이 2차원 리스트를 다룰 때 어떻게 결합되어 쓰이는지 원리를 이해하였으며, 제공된 뼈대 코드를 바탕으로 직접 프로그램을 실행하고 정상 작동하는지 테스트함.

- 반영 위치:main.py

---

## 3차 Gemini 활용

- 사용 날짜:
- 사용 목적:
- 내가 입력한 프롬프트:

```text

```

- Gemini가 제안한 내용 요약:
- 내가 반영한 부분:
- 내가 수정하거나 사용하지 않은 부분:
- 반영 위치:

---

## AI 활용 성찰

Gemini가 도움이 된 점:


Gemini의 제안을 그대로 쓰지 않고 내가 판단하거나 수정한 점:


다음에 AI를 사용할 때 주의해야 할 점:

